version: "3.9"

services:
  gitlab-runner:
    image: gitlab/gitlab-runner:${GITLAB_RUNNER_TAG:-alpine-v19.2.0}
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - gitlab-runner-config:/etc/gitlab-runner
    healthcheck:
      test: ["CMD-SHELL", "gitlab-runner --version || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5

volumes:
  gitlab-runner-config:
    name: gitlab-runner-config-${GITLAB_RUNNER_TAG:-alpine-v19.2.0}

