version: "3.9"

services:
  kibana:
    image: docker.elastic.co/kibana/kibana:${ELASTIC_TAG:-9.5.0}
    environment:
      ELASTICSEARCH_HOSTS: "http://elasticsearch:9200"
    ports:
      - "5601:5601"
    depends_on:
      - elasticsearch

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:${ELASTIC_TAG:-9.5.0}
    environment:
      discovery.type: single-node
      xpack.security.enabled: "false"
      ES_JAVA_OPTS: "-Xms1g -Xmx1g"
    ports:
      - "9202:9200"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - es-kibana-data:/usr/share/elasticsearch/data

volumes:
  es-kibana-data:
    name: es-kibana-data-${ELASTIC_TAG:-9.5.0}

